<h1>
  <%= @category.name %>
</h1>

<%= render 'products/table', products: @category.products %>
<p>
</p>

<% if current_user && current_user.admin? %>
  <%= link_to 'Edit', edit_category_path(@category) %> |
<% end %>
<%= link_to 'Back', categories_path %>
