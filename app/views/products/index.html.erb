<h1> Listing products </h1>

<%= render 'table', products: @products %>
<br></br>

<% if current_user && current_user.admin? %>
  <p>
    <%= link_to 'Food', new_product_path, class: "btn btn-primary"%>
  </p>
<% end %>
